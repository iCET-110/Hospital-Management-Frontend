import { HoldPatientService } from './hold-patient-service';

describe('HoldPatientService', () => {
  it('should create an instance', () => {
    expect(new HoldPatientService()).toBeTruthy();
  });
});
